# Xcode
build/
DerivedData/
*.xcuserstate
*.xcuserdatad/
xcuserdata/
*.xcscmblueprint
*.xccheckout

# SPM
.swiftpm/
.build/

# macOS
.DS_Store

# Simulator / device logs
*.ips

# CloudConvert API key (DESIGN_SPEC §5/§9 "Office format export" — test
# integration) — never commit the real key; see CloudConvertConfig.swift.example.
DocumentScanner/Services/CloudConvertConfig.swift
